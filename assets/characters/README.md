# Character Assets

David is built procedurally in `src/player.js` as a polished stylized cartoon shepherd
(hierarchical meshes with joint groups for animation).

Future levels can place glTF / GLB models here and load them via THREE.GLTFLoader.
Folder structure is ready for:
- david.glb
- goliath.glb
- shadow_guardian.glb
